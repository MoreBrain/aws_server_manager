# Target host for ./deploy.sh
HOST_IP=192.168.0.10
SERVER_USER=nsc
SERVER_PORT=22
REMOTE_PATH=/home/nsc/aws_server_manager

# Optional: AWS profile used by the backend container (defaults to account_nsc)
# AWS_PROFILE=account_nsc
